let arr = [1, 2, 3, 4, 5];
//let arr = [100, 101, 102];

let sum = 0;

for (let i = 0; i < arr.length; i ++) {
    sum = sum + arr[i];
  }

  console.log(sum);

  // console.log(arr[0] + arr[1] + arr[2] + arr[3] + arr[4]);
